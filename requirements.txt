# ── Web framework ────────────────────────────────────────────────────────
fastapi==0.115.0
gunicorn==22.0.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12    # required for OAuth2PasswordRequestForm

# ── Database ─────────────────────────────────────────────────────────────
sqlalchemy==2.0.35
psycopg2-binary==2.9.9      # PostgreSQL driver

# ── Auth ─────────────────────────────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── Validation / settings ───────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0      # required for Pydantic EmailStr

# ── ML / inference (ported from the Streamlit app) ─────────────────────
torch==2.3.1
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.5.1
joblib==1.4.2
requests==2.32.3

# ── LLM assistant — free tier, not Anthropic/OpenAI (no paid API access) ─
google-generativeai==0.8.3
