# This package will hold the ported model architecture, feature engineering,
# and geocoding logic (StormSentinelNetV2, inference pipeline, location
# search) — next build phase, wraps the validated v2 model from training.
